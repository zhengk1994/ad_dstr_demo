# Requirement

* ruby -> w1のREADME.mdを参照してインストールしてください。
* rspec -> `gem install rspec`

# 宿題

* `sort.rb`は実行できるけど、その中でいくつかのバグがあります。
* `spec/sort_spec.rb`のサンプルをみながら、`sort.rb`のバグに対してテストコードを書
   いてください。
